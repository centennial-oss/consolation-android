<?xml version="1.0" encoding="utf-8"?>
<!-- USB_DEVICE_ATTACHED filter: matching devices get implicit host permission on each attach
     (Android does not persist explicit requestPermission() grants across disconnect).
     Add more <usb-device> lines for your VID/PID (0x-prefixed hex or decimal, see DeviceFilter). -->
<resources>
    <usb-device vendor-id="0x534d" />
    <usb-device vendor-id="0x345F" />
    <usb-device vendor-id="0xFD9" />
</resources>
